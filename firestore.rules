
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ACCÈS TOTAL ET ILLIMITÉ - TOUTES PERMISSIONS SAUTÉES
    // Autorise absolument toute lecture et écriture sans aucune condition
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
